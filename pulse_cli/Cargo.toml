[package]
name = "pulse_cli"
version = "0.1.0"
edition = "2021"

[dependencies]
pulse_core = { path = "../pulse_core" }
pulse_vm = { path = "../pulse_vm" }
pulse_compiler = { path = "../pulse_compiler" }
pulse_runtime = { path = "../pulse_runtime" }
