[package]
name = "pulse_core"
version = "0.1.0"
edition = "2021"


[dependencies]
serde = { version = "1.0", features = ["derive", "rc"] }
bincode = "1.3"
bytes = "1.5"

async-trait = "0.1"
futures = "0.3" 
tokio = { version = "1.0", features = ["net", "rt", "macros", "sync"] }
