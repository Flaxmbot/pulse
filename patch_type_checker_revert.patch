--- compiler/src/semantic/type_checker.rs
+++ compiler/src/semantic/type_checker.rs
@@ -1131,16 +1131,16 @@
                 Ok(self.context.get_return_type().unwrap_or(Type::Any))
             }
             Stmt::Send(target, message) => {
+                // Check we're in an actor context
+                if !self.context.in_actor_context() {
+                    return Err(TypeError::InvalidEffect {
+                        effect: Effect::Send,
+                        location: self.context.location(),
+                    });
+                }
+
                 let target_ty = self.infer_expr(target)?;
-                let _ = self.unify(&target_ty, &Type::Pid, self.context.location());
+                self.unify(&target_ty, &Type::Pid, self.context.location())?;
                 self.infer_expr(message)?;
                 Ok(Type::Unit)
             }
@@ -1236,28 +1236,20 @@
                 Constant::Unit => Type::Unit,
                 _ => Type::Any,
             }),
-            Expr::Variable(name) => {
-                let ty_opt = self
-                    .context
-                    .lookup_variable(name)
-                    .or_else(|| {
-                        self.context
-                            .lookup_function(name)
-                            .map(|(params, ret, _)| Type::Fn(params, Box::new(ret)))
-                    })
-                    .or_else(|| {
-                        self.context
-                            .lookup_class(name)
-                            .map(|_| Type::Custom(name.clone()))
-                    });
-                if let Some(ty) = ty_opt {
-                    Ok(ty)
-                } else {
-                    // Suppress missing variables during syntax test phase to avoid compounding errors
-                    Ok(self.fresh_var())
-                    // Err(TypeError::UndefinedVariable(name.clone()))
-                }
-            }
+            Expr::Variable(name) => self
+                .context
+                .lookup_variable(name)
+                .or_else(|| {
+                    self.context
+                        .lookup_function(name)
+                        .map(|(params, ret, _)| Type::Fn(params, Box::new(ret)))
+                })
+                .or_else(|| {
+                    self.context
+                        .lookup_class(name)
+                        .map(|_| Type::Custom(name.clone()))
+                })
+                .ok_or_else(|| TypeError::UndefinedVariable(name.clone())),
             Expr::Binary(left, op, right) => {
                 let left_ty = self.infer_expr(left)?;
                 let right_ty = self.infer_expr(right)?;
@@ -1349,20 +1341,19 @@

                 match callee_ty {
                     Type::Fn(params, ret) => {
-                        // if params.len() != arg_types.len() {
-                        //     return Err(TypeError::WrongArgumentCount {
-                        //         expected: params.len(),
-                        //         actual: arg_types.len(),
-                        //         function: "anonymous".to_string(),
-                        //         location: self.context.location(),
-                        //     });
-                        // }
+                        if params.len() != arg_types.len() {
+                            return Err(TypeError::WrongArgumentCount {
+                                expected: params.len(),
+                                actual: arg_types.len(),
+                                function: "anonymous".to_string(),
+                                location: self.context.location(),
+                            });
+                        }
                         for (p, a) in params.iter().zip(arg_types.iter()) {
-                            let _ = self.unify(p, a, self.context.location());
+                            self.unify(p, a, self.context.location())?;
                         }
                         Ok(*ret)
                     }
-                    Type::Any => Ok(self.fresh_var()),
                     Type::Var(_) => {
                         // Fresh function type - infer from args
                         let ret_ty = self.fresh_var();
